# Roadmap

## v0.1 — control plane scaffold
- enrollment
- device presence
- session request

## v0.2 — deployable Linux appliance
- SQLite persistence
- split secrets
- systemd
- TLS proxy templates
- TURN template

## v0.3 — working remote-control prototype
- authenticated per-session relay
- live Windows desktop frames
- controller viewer
- mouse/keyboard return path
- GitHub Actions binary builds

## v0.4 — low-latency transport
- DXGI Desktop Duplication
- dirty-region awareness
- hardware H.264
- WebRTC peer connection
- ICE/STUN
- TURN UDP/TLS fallback
- data channels for input/control
- server becomes signaling-only during normal sessions

## v0.5 — unattended Windows product
- Windows service
- logged-in-session helper
- UAC/secure desktop strategy
- multi-monitor
- clipboard
- signed update channel
- RBAC/device policies
- audit viewer
